[project]
requires-python = ">= 3.13"
name = "container-status-logger"
description = "logs container status to stdout"
authors = [{ name = "Brad Holland", email = "hollanbm@gmail.com" }]
version = "1"
dynamic = ["dependencies"]

[tool.poetry]
requires-poetry = ">=2.0"
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"

[tool.poetry.group.dev.dependencies]
ruff = "^0.12.0"
